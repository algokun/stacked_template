import 'package:flutter/material.dart';

/// Background Colors
/// The [background color] appears behind scrollable content.
const Color backgroundLight = Color(0xFFF8F8F8);
const Color backgroundDark = Color(0xFF12181b);

/// Primary and Secondary Colors
/// A [primary color] is the color displayed most frequently
///   across your app's screens and components.
/// A [secondary color] provides more ways to accent and distinguish your product.
/// Having a secondary color is optional, and should be applied sparingly
///  to accent select parts of your UI.
const Color primary = Color(0xFF09c372);
const Color secondary = Color(0xFFfa8142);
const Color error = Color(0xFFDA595A);

/// Surface Colors
/// Surface colors affect surfaces of components, such as cards, sheets, and menus.
const Color surfaceLight = Color(0xFFFFFFFF);
const Color surfaceDark = Color(0xFF2a2e35);

// Text Colors

const Color textSecondaryLight = Color(0xFF454e56);
const Color textSecondaryDark = Color(0xFFadbecd);
